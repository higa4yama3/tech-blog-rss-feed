---
layout: layouts/main.njk
eleventyComputed:
    pageTitle: "人気フィード｜{{ constants.siteTitle }}"
    lastUpdated: "{{ lastModifiedBlogsDate }}"
---

{% include 'layouts/partials/nav.njk' %}

<section class="ui-section-content ui-section-feed">
    <div class="ui-layout-container">
        <h2 class='ui-typography-heading'>直近1週間の人気フィード</h2>
        <p class='ui-text-note'>はてブ{{ constants.discoverMinHatenaCount }}件以上・直近{{ constants.discoverWindowDays }}日（<a href="{{ constants.feedUrls.discover }}">Discover RSS</a>と同内容）</p>

        <div class="ui-section-content--feature ui-layout-grid ui-layout-grid-3 ui-container-feed ui-container-feed--hot">
            {% asyncEach feedItem in feedItemsHot %}
                {% if loop.index <= 4 %}
                    {% set imageLoading = 'eager' %}
                {% else %}
                    {% set imageLoading = 'lazy' %}
                {% endif %}
                {% include 'layouts/partials/feed-item.njk' %}
            {% endeach %}
        </div>
    </div>
</section>

<script>
    {% include "scripts/index.ts" %}
</script>
